#include<iostream>
using namespace std;
int main()
{
	int y,n;
	int steps=0;
	cin >> y;
	if (y <= 5)cout << "1";
	else 
		if (y % 5 == 0)
	    {
		    n = y / 5;
		    cout << n;
	    }
	    else
		{
			n = y / 5 + 1;
			cout << n;
	    }
 
}

